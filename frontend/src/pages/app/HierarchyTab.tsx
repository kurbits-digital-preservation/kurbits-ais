// HierarchyTab renders the full hierarchy management UI
// inside the Administration page as a tab.
// The HierarchyPage component is self-contained so we just re-use it directly.
import HierarchyPage from './HierarchyPage'

export default function HierarchyTab() {
  return <HierarchyPage />
}